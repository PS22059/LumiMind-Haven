/** @type {import('next').NextConfig} */
const nextConfig = {
    distDir: "dist",
    images: {
        unoptimized: true,
    },
    reactStrictMode: true,
    basePath: "//LumiMind-Haven",
};

export default nextConfig;
